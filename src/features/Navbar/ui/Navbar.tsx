import {Button, ButtonTheme} from '@/shared/ui/Button';

export const Navbar = () => {
    return (
        <div>
            <Button theme={ButtonTheme.OUTLINED}>
                Sign Up
            </Button>
        </div>
    )
}
