# Secrets for MCP servers. NEVER commit this file.
# Filled in manually by the developer.

# Supabase project ref — the "<ref>" part of https://<ref>.supabase.co
SUPABASE_PROJECT_REF=

# Supabase Personal Access Token (sbp_...) — generate at:
# https://supabase.com/dashboard/account/tokens
# This is NOT the anon key from .env.
SUPABASE_ACCESS_TOKEN=
